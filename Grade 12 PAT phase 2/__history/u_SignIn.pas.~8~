unit u_SignIn;

interface
uses
u_passwordhasher,dm_CO2;
type
  SignIn = class(tObject)
  private
    fFirst_name: string;
    flast_name: string;
    fUsername: String;
    fPassword: string;
    fAttempts:string;
  public
  procedure SetLogin(sUsername,sPassword:string);
  Function PasswordCheck: boolean;
  end;

implementation

{ SignIn }

function SignIn.PasswordCheck: boolean;
begin
with dm_CO2 do
while not tblUsers.tblUsers.eof do

if hash(fPassword)= then

end;

procedure SignIn.SetLogin(sUsername, sPassword: string);
begin
fUsername:=sUsername;
fPassword:=sPassword;
end;



end.
